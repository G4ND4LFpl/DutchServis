@model IEnumerable<DutchServisMCV.Models.PlayerInfo>

@{
    ViewBag.Title = "Lista graczy";
}

<script type="text/jscript" src="@Url.Content("~/Scripts/dynamic_table.js")"></script>

<header>
    <h1 class="mb-1">Lista graczy</h1>
</header>

<div class="row mb-1">
    <div class="col-xs-12 col-sm-4 col-md-6 col-lg-8">
        @if (Session["Username"] != null)
        {
            <aside>
                <a href="@Url.Action("Add")">
                    <div class="admincontroll">Dodaj gracza</div>
                </a>
            </aside>
        }
    </div>
    <div class="col-xs-12 col-sm-8 col-md-6 col-lg-4">
        <div class="row">
            <div class="col-xs-12">
                <button class="w-full" onclick="openFilterMenu()">Filtry</button>
            </div>
        </div>
        <div class="graybox" id="filter_menu">
            <div class="row gutters-5">
                <div class="col-xs-6">
                    <label>Nick gracza</label>
                </div>
                <div class="col-xs-6">
                    <input class="text-box single-line" id="filter_filed" type="text" onchange="refresh()" />
                </div>
            </div>
            <div class="row gutters-5">
                <div class="col-xs-6">
                    <label>Pokaż nieaktywnych</label>
                </div>
                <div class="col-xs-6">
                    <input id="active_checkbox" type="checkbox" name="OnlyActive" onchange="refresh()" />
                </div>
            </div>
        </div>
    </div>
</div>

<table class="table table-striped">
    <thead class="table-style">
        <tr>
            <th class="cell-1 disp-sm">Pozycja</th>
            <th class="cell-1 disp-only-xs">Poz.</th>
            <th class="cell-1 disp-md">Awatar</th>
            <th class="cell-2">
                <input class="sortbtn" type="button" id="player_sort" value="Gracz" onclick="sortPlayer()" />
            </th>
            <th class="cell-2 disp-lg">Klan</th>
            <th class="cell-1 disp-sm">
                <input class="sortbtn" type="button" id="rank_sort1" value="Ranking" onclick="sortRanking()" />
            </th>
            <th class="cell-1 disp-only-xs">
                <input class="sortbtn" type="button" id="rank_sort2" value="Rank." onclick="sortRanking()" />
            </th>
            <th class="cell-1 disp-sm">
                <input class="sortbtn" type="button" id="elo_sort1" value="Rating" onclick="sortRating()" />
            </th>
            <th class="cell-1 disp-only-xs">
                <input class="sortbtn" type="button" id="elo_sort2" value="Elo" onclick="sortRating()" />
            </th>
        </tr>
    </thead>

    <tbody id="table_body" class="table-style h-100">
        <!-- Javascript generated content -->
    </tbody>
</table>

<script type="text/jscript">
    var model = @Html.Raw(Json.Encode(Model));
    var path = @Url.Content("~/Content/");

    sortRanking();
</script>